% first design building.
% @param algorithm object handling options, status, ...
% @param d the number of variables all set in [0,1]
% @return matrix of first design step
function [X] = getInitialDesign(algorithm,d)
    ...
end
